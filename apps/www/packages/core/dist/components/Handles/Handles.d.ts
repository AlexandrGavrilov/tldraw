import type { TLShape } from '../../types';
interface HandlesProps {
    shape: TLShape;
    zoom: number;
}
export declare const Handles: (({ shape, zoom }: HandlesProps) => JSX.Element | null) & {
    displayName: string;
};
export {};
//# sourceMappingURL=Handles.d.ts.map